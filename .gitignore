# Claude Code session data & worktrees
.claude/

# OS
.DS_Store

# Cấu hình deploy chứa thông tin server — không commit.
/deploy.config

# Biến môi trường production (secret, DB pass) — không commit.
/.env

# Playwright MCP artifacts
.playwright-mcp/
/backend/config/dev.local.exs.example
